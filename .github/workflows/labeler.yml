name: PR Labeler

on:
  - pull_request_target
  workflow_dispatch:
    inputs:
      PR:
        description: PR Number
        required: true

jobs:
  label:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/labeler@v2
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"